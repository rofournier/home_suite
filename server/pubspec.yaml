name: home_sweet_home_server
description: Serveur local (auth + courses temps réel) pour Home Sweet Home.
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.1
  sqlite3: ^2.4.6
  dart_jsonwebtoken: ^2.14.0
  bcrypt: ^1.1.3
  uuid: ^4.5.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  http: ^1.2.0
